name: Auto-Fix CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-fix:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v6
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run lint
      run: npm run lint --if-present || true
    
    - name: Run lint fix
      run: npm run lint:fix --if-present || true
    
    - name: Run format
      run: npm run format --if-present || true
    
    - name: Run build
      run: npm run build --if-present || true
    
    - name: Run tests
      run: npm test --if-present || true
    
    - name: Check for changes
      id: verify-changed-files
      run: |
        if [ -n "$(git status --porcelain)" ]; then
          echo "changed=true" >> $GITHUB_OUTPUT
        else
          echo "changed=false" >> $GITHUB_OUTPUT
        fi
    
    - name: Commit and push changes
      if: steps.verify-changed-files.outputs.changed == 'true' && github.event_name == 'push' && matrix.node-version == '20.x'
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add .
        git commit -m "Auto-fix: Apply automated fixes"
        git push
    
    - name: Create Pull Request
      if: steps.verify-changed-files.outputs.changed == 'true' && github.event_name == 'pull_request' && matrix.node-version == '20.x'
      uses: peter-evans/create-pull-request@v5
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        commit-message: 'Auto-fix: Apply automated fixes'
        title: 'Auto-fix: Apply automated fixes'
        body: 'This PR applies automated fixes from linting, formatting, and other automated tools.'
        branch: auto-fix-${{ github.run_id }}
